import SwiftUI

struct PageTitle: View {
    var body: some View {
        VStack {
            Text("Welcome to Chapters")
                .font(.largeTitle)
                .bold()
        }
    }
}
